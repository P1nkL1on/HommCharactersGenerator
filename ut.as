
	// constants and enums
	
	class ut{
	
		static function atLeast(X, value):Boolean{
			trace(X+'/'+value)
			return X >= value;
		}
		static function atMax (X, value):Boolean{
			return X <= value;
		}
	
	}